name: Hybrid runtime scan (self-hosted)
on:
  push:
    branches:
      - main
      - feat/normalize
    paths:
      - 'iac/terraform/**'
      - 'openstack/**'
      - 'src/**'
      - 'ansible/**'
      - 'policy/**'
      - '.github/workflows/hybrid_runtime_scan.yml'
  workflow_dispatch:
    inputs:
      include_aws_scan:
        description: "Run AWS runtime + IaC scanners on the self-hosted runner"
        required: false
        default: "true"
      include_openstack_scan:
        description: "Export OpenStack evidence and normalize it on the self-hosted runner"
        required: false
        default: "true"
      include_openstack_object_storage:
        description: "Include the M1 OpenStack object storage/Swift scenario during export and normalization"
        required: false
        default: "false"
      publish_to_siem:
        description: "Publish findings, triage, and remediation events to Elasticsearch"
        required: false
        default: "true"
      dispatch_live_integrations:
        description: "Send triage notifications to Jira/ServiceNow/Slack/Teams"
        required: false
        default: "false"
      enable_runtime_remediation:
        description: "Execute runtime remediation for AWS/OpenStack findings"
        required: false
        default: "false"
      reconcile_m5_drift:
        description: "Run the M5 Terraform drift reconcile flow"
        required: false
        default: "false"
      simulate_m5_drift:
        description: "Create the M5 drift before reconciling it"
        required: false
        default: "false"
      generate_iac_pr:
        description: "Generate the IaC remediation PR bundle"
        required: false
        default: "false"
      force_rds_cutover:
        description: "Force the M4 encrypted RDS replacement cutover"
        required: false
        default: "false"
      delete_archived_rds:
        description: "Delete the archived pre-cutover RDS instance after force cutover"
        required: false
        default: "false"
      openrc_path:
        description: "Path to the OpenStack RC file on the self-hosted runner"
        required: false
        default: "~/openrc"
      openstack_activate_path:
        description: "Path to the OpenStack CLI/Kolla virtualenv activation script on the self-hosted runner"
        required: false
        default: "~/kolla-venv/bin/activate"
      project_prefix:
        description: "Resource prefix shared across the AWS/OpenStack demo lab"
        required: false
        default: "threat-demo"
      aws_region:
        description: "AWS region for runtime scanners and remediation"
        required: false
        default: "ap-southeast-1"
      pipeline_source:
        description: "Pipeline source tag stored in Elasticsearch"
        required: false
        default: "github-self-hosted-hybrid"

jobs:
  hybrid-runtime-scan:
    runs-on:
      - self-hosted
      - linux
      - openstack
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    env:
      ELASTICSEARCH_HOST: ${{ secrets.ELASTICSEARCH_HOST }}
      ELASTICSEARCH_PORT: ${{ secrets.ELASTICSEARCH_PORT }}
      ELASTICSEARCH_SCHEME: ${{ secrets.ELASTICSEARCH_SCHEME }}
      CFG_INCLUDE_AWS_SCAN: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.include_aws_scan || 'true' }}
      CFG_INCLUDE_OPENSTACK_SCAN: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.include_openstack_scan || 'true' }}
      CFG_INCLUDE_OPENSTACK_OBJECT_STORAGE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.include_openstack_object_storage || 'false' }}
      CFG_PUBLISH_TO_SIEM: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.publish_to_siem || 'true' }}
      CFG_DISPATCH_LIVE_INTEGRATIONS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.dispatch_live_integrations || 'false' }}
      CFG_ENABLE_RUNTIME_REMEDIATION: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.enable_runtime_remediation || 'false' }}
      CFG_RECONCILE_M5_DRIFT: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.reconcile_m5_drift || 'false' }}
      CFG_SIMULATE_M5_DRIFT: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.simulate_m5_drift || 'false' }}
      CFG_GENERATE_IAC_PR: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.generate_iac_pr || 'false' }}
      CFG_FORCE_RDS_CUTOVER: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.force_rds_cutover || 'false' }}
      CFG_DELETE_ARCHIVED_RDS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.delete_archived_rds || 'false' }}
      CFG_OPENRC_PATH: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.openrc_path || '~/openrc' }}
      CFG_OPENSTACK_ACTIVATE_PATH: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.openstack_activate_path || '~/kolla-venv/bin/activate' }}
      CFG_PROJECT_PREFIX: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.project_prefix || 'threat-demo' }}
      CFG_AWS_REGION: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.aws_region || 'ap-southeast-1' }}
      CFG_PIPELINE_SOURCE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.pipeline_source || 'github-self-hosted-hybrid' }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Set up Terraform
        if: env.CFG_RECONCILE_M5_DRIFT == 'true'
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: "1.5.0"

      - name: Install base dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install -r requirements.txt
          python -m pip install -r requirements-dev.txt

      - name: Verify Python runtime dependencies
        run: |
          python -c "import dotenv, pydantic, elasticsearch; print('base runtime ok')"

      - name: Run unit tests
        run: |
          python -m pytest tests

      - name: Install Checkov and ScoutSuite into isolated venvs
        run: |
          python -m venv /tmp/checkov-venv
          /tmp/checkov-venv/bin/pip install --upgrade pip
          /tmp/checkov-venv/bin/pip install -r requirements-checkov.txt

          python -m venv /tmp/scoutsuite-venv
          /tmp/scoutsuite-venv/bin/pip install --upgrade pip
          /tmp/scoutsuite-venv/bin/pip install -r requirements-scoutsuite.txt

      - name: Install tfsec and Trivy on the runner PATH
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -sSfL https://raw.githubusercontent.com/aquasecurity/tfsec/master/scripts/install_linux.sh | bash
          TFSEC_BIN="$(command -v tfsec)"
          test -n "$TFSEC_BIN"
          echo "$(dirname "$TFSEC_BIN")" >> "$GITHUB_PATH"
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b "$HOME/.local/bin"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          "$TFSEC_BIN" --version

      - name: Install remediation toolchain
        if: env.CFG_ENABLE_RUNTIME_REMEDIATION == 'true' || env.CFG_RECONCILE_M5_DRIFT == 'true' || env.CFG_GENERATE_IAC_PR == 'true'
        run: |
          python -m venv /tmp/remediation-venv
          /tmp/remediation-venv/bin/pip install --upgrade pip
          /tmp/remediation-venv/bin/pip install -r requirements-remediation.txt
          curl -L -o "$HOME/.local/bin/opa" https://openpolicyagent.org/downloads/latest/opa_linux_amd64_static
          chmod +x "$HOME/.local/bin/opa"

      - name: Verify OpenStack authentication
        if: env.CFG_INCLUDE_OPENSTACK_SCAN == 'true' || env.CFG_ENABLE_RUNTIME_REMEDIATION == 'true'
        run: |
          OPENSTACK_ACTIVATE_PATH="${CFG_OPENSTACK_ACTIVATE_PATH}"
          OPENRC_PATH="${CFG_OPENRC_PATH}"
          if [[ -n "${OPENSTACK_ACTIVATE_PATH}" ]]; then
            test -f "${OPENSTACK_ACTIVATE_PATH/#\~/$HOME}"
            source "${OPENSTACK_ACTIVATE_PATH/#\~/$HOME}"
          fi
          test -f "${OPENRC_PATH/#\~/$HOME}"
          set -a
          source "${OPENRC_PATH/#\~/$HOME}"
          set +a
          openstack token issue -f value -c id >/dev/null

      - name: Verify AWS authentication
        if: env.CFG_INCLUDE_AWS_SCAN == 'true' || env.CFG_ENABLE_RUNTIME_REMEDIATION == 'true' || env.CFG_RECONCILE_M5_DRIFT == 'true'
        env:
          AWS_REGION: ${{ env.CFG_AWS_REGION }}
        run: |
          aws sts get-caller-identity >/dev/null

      - name: Run AWS scanners
        if: env.CFG_INCLUDE_AWS_SCAN == 'true'
        env:
          AWS_REGION: ${{ env.CFG_AWS_REGION }}
        run: |
          PYTHON_BIN="$(command -v python)"
          export PATH="/tmp/checkov-venv/bin:/tmp/scoutsuite-venv/bin:$HOME/.local/bin:$PATH"
          mkdir -p ./scan_results/aws
          "$PYTHON_BIN" -m src.scanners.runner \
            --scoutsuite \
            --cloudsploit \
            --checkov \
            --tfsec \
            --trivy \
            --terraform-dir ./iac/terraform \
            --trivy-scan-ref ./iac/terraform \
            --output-dir ./scan_results/aws

      - name: Export and normalize OpenStack findings
        if: env.CFG_INCLUDE_OPENSTACK_SCAN == 'true'
        run: |
          PYTHON_BIN="$(command -v python)"
          OPENSTACK_ACTIVATE_PATH="${CFG_OPENSTACK_ACTIVATE_PATH}"
          OPENRC_PATH="${CFG_OPENRC_PATH}"
          if [[ -n "${OPENSTACK_ACTIVATE_PATH}" ]]; then
            test -f "${OPENSTACK_ACTIVATE_PATH/#\~/$HOME}"
            source "${OPENSTACK_ACTIVATE_PATH/#\~/$HOME}"
          fi
          set -a
          source "${OPENRC_PATH/#\~/$HOME}"
          set +a
          PROJECT_PREFIX="${CFG_PROJECT_PREFIX}" \
          OPENSTACK_INCLUDE_OBJECT_STORAGE="${CFG_INCLUDE_OPENSTACK_OBJECT_STORAGE}" \
            bash ./openstack/export_misconfig_openstack.sh
          FINDING_ARGS=()
          if [[ -f ./reports/raw/openstack/live/container_public.json ]]; then
            FINDING_ARGS+=(--container-file ./reports/raw/openstack/live/container_public.json)
          fi
          if [[ -f ./reports/raw/openstack/live/security_group_rules.json ]]; then
            FINDING_ARGS+=(--sg-rules-file ./reports/raw/openstack/live/security_group_rules.json)
          fi
          if [[ -f ./reports/raw/openstack/live/role_assignments.json ]]; then
            FINDING_ARGS+=(--role-assignments-file ./reports/raw/openstack/live/role_assignments.json)
          fi
          "$PYTHON_BIN" -m src.openstack.findings \
            "${FINDING_ARGS[@]}" \
            --output ./scan_results/openstack_findings.json

      - name: Merge AWS and OpenStack findings into one hybrid batch
        run: |
          MERGE_INPUTS=()
          if [[ -f ./scan_results/aws/findings.json ]]; then
            MERGE_INPUTS+=(./scan_results/aws/findings.json)
          fi
          if [[ -f ./scan_results/openstack_findings.json ]]; then
            MERGE_INPUTS+=(./scan_results/openstack_findings.json)
          fi
          if [[ "${#MERGE_INPUTS[@]}" -eq 0 ]]; then
            echo "No findings were generated by the selected scanners." >&2
            exit 1
          fi
          python scripts/merge_json_collections.py \
            --inputs "${MERGE_INPUTS[@]}" \
            --output ./scan_results/hybrid_findings.json

      - name: Triage hybrid findings
        run: |
          python -m src.triage.engine \
            --input ./scan_results/hybrid_findings.json \
            --output ./triage_results/hybrid_decisions.json

      - name: Generate hybrid notification artifacts
        env:
          JIRA_URL: ${{ secrets.JIRA_URL }}
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_USERNAME: ${{ secrets.JIRA_USERNAME }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}
          JIRA_PROJECT_KEY: ${{ secrets.JIRA_PROJECT_KEY }}
          JIRA_ISSUE_TYPE: ${{ secrets.JIRA_ISSUE_TYPE }}
          SERVICENOW_URL: ${{ secrets.SERVICENOW_URL }}
          SERVICENOW_USER: ${{ secrets.SERVICENOW_USER }}
          SERVICENOW_PASSWORD: ${{ secrets.SERVICENOW_PASSWORD }}
          SERVICENOW_TABLE: ${{ secrets.SERVICENOW_TABLE }}
          SERVICENOW_CATEGORY: ${{ secrets.SERVICENOW_CATEGORY }}
          SERVICENOW_SUBCATEGORY: ${{ secrets.SERVICENOW_SUBCATEGORY }}
          SERVICENOW_ASSIGNMENT_GROUP_SYS_ID: ${{ secrets.SERVICENOW_ASSIGNMENT_GROUP_SYS_ID }}
          SERVICENOW_CALLER_SYS_ID: ${{ secrets.SERVICENOW_CALLER_SYS_ID }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
        run: |
          NOTIFICATION_ARGS=()
          if [[ "${CFG_DISPATCH_LIVE_INTEGRATIONS}" == "true" ]]; then
            NOTIFICATION_ARGS+=(--dispatch-live --fail-on-dispatch-error)
          fi
          python -m src.triage.notifications \
            --findings ./scan_results/hybrid_findings.json \
            --decisions ./triage_results/hybrid_decisions.json \
            --output-dir ./artifacts/triage_notifications \
            "${NOTIFICATION_ARGS[@]}"

      - name: Verify Elasticsearch availability
        if: env.CFG_PUBLISH_TO_SIEM == 'true'
        run: |
          ES_HOST="${ELASTICSEARCH_HOST:-localhost}"
          ES_PORT="${ELASTICSEARCH_PORT:-9200}"
          ES_SCHEME="${ELASTICSEARCH_SCHEME:-http}"
          curl -fsS "${ES_SCHEME}://${ES_HOST}:${ES_PORT}" >/dev/null

      - name: Publish hybrid findings and triage to Elasticsearch
        if: env.CFG_PUBLISH_TO_SIEM == 'true'
        run: |
          python -m src.siem.publisher \
            --findings ./scan_results/hybrid_findings.json \
            --decisions ./triage_results/hybrid_decisions.json \
            --pipeline-source "${CFG_PIPELINE_SOURCE}" \
            --branch "${GITHUB_REF_NAME}" \
            --commit-sha "${GITHUB_SHA}"

      - name: Execute AWS runtime remediation
        if: env.CFG_ENABLE_RUNTIME_REMEDIATION == 'true' && env.CFG_INCLUDE_AWS_SCAN == 'true'
        env:
          AWS_REGION: ${{ env.CFG_AWS_REGION }}
        run: |
          PYTHON_BIN="$(command -v python)"
          export PATH="/tmp/remediation-venv/bin:$HOME/.local/bin:$PATH"
          RDS_ARGS=()
          if [[ "${CFG_FORCE_RDS_CUTOVER}" == "true" ]]; then
            RDS_ARGS+=(--force-rds-cutover)
          fi
          if [[ "${CFG_DELETE_ARCHIVED_RDS}" == "true" ]]; then
            RDS_ARGS+=(--delete-archived-rds)
          fi
          "$PYTHON_BIN" -m src.remediation.aws_runtime_executor \
            --findings ./scan_results/hybrid_findings.json \
            --decisions ./triage_results/hybrid_decisions.json \
            --region "${AWS_REGION}" \
            --project-prefix "${CFG_PROJECT_PREFIX}" \
            --approve-all-manual \
            --execute \
            --pipeline-source "${CFG_PIPELINE_SOURCE}-aws-runtime" \
            --branch "${GITHUB_REF_NAME}" \
            --commit-sha "${GITHUB_SHA}" \
            "${RDS_ARGS[@]}"

      - name: Execute OpenStack runtime remediation
        if: env.CFG_ENABLE_RUNTIME_REMEDIATION == 'true' && env.CFG_INCLUDE_OPENSTACK_SCAN == 'true'
        run: |
          PYTHON_BIN="$(command -v python)"
          OPENSTACK_ACTIVATE_PATH="${CFG_OPENSTACK_ACTIVATE_PATH}"
          OPENRC_PATH="${CFG_OPENRC_PATH}"
          if [[ -n "${OPENSTACK_ACTIVATE_PATH}" ]]; then
            test -f "${OPENSTACK_ACTIVATE_PATH/#\~/$HOME}"
            source "${OPENSTACK_ACTIVATE_PATH/#\~/$HOME}"
          fi
          set -a
          source "${OPENRC_PATH/#\~/$HOME}"
          set +a
          "$PYTHON_BIN" -m src.remediation.runtime_executor \
            --findings ./scan_results/hybrid_findings.json \
            --decisions ./triage_results/hybrid_decisions.json \
            --provider openstack \
            --approve-all-manual \
            --execute \
            --output-events ./artifacts/remediation/openstack_runtime_events.json \
            --output-findings-after ./artifacts/remediation/openstack_findings_after_runtime.json \
            --pipeline-source "${CFG_PIPELINE_SOURCE}-openstack-runtime" \
            --branch "${GITHUB_REF_NAME}" \
            --commit-sha "${GITHUB_SHA}"

      - name: Reconcile M5 drift
        if: env.CFG_RECONCILE_M5_DRIFT == 'true'
        env:
          AWS_REGION: ${{ env.CFG_AWS_REGION }}
        run: |
          PYTHON_BIN="$(command -v python)"
          export PATH="/tmp/remediation-venv/bin:$HOME/.local/bin:$PATH"
          DRIFT_ARGS=()
          if [[ "${CFG_SIMULATE_M5_DRIFT}" == "true" ]]; then
            DRIFT_ARGS+=(--simulate-drift)
          fi
          "$PYTHON_BIN" -m src.remediation.drift_reconcile \
            --terraform-dir ./iac/terraform \
            --region "${AWS_REGION}" \
            --project-prefix "${CFG_PROJECT_PREFIX}" \
            --execute \
            --pipeline-source "${CFG_PIPELINE_SOURCE}-m5-drift" \
            --branch "${GITHUB_REF_NAME}" \
            --commit-sha "${GITHUB_SHA}" \
            --output-dir ./artifacts/drift/m5 \
            "${DRIFT_ARGS[@]}"

      - name: Generate IaC remediation PR bundle
        if: env.CFG_GENERATE_IAC_PR == 'true'
        run: |
          PYTHON_BIN="$(command -v python)"
          export PATH="/tmp/remediation-venv/bin:$HOME/.local/bin:$PATH"
          "$PYTHON_BIN" -m src.remediation.iac_pr_prepare \
            --findings ./scan_results/hybrid_findings.json \
            --decisions ./triage_results/hybrid_decisions.json \
            --terraform-dir ./iac/terraform \
            --output-dir ./artifacts/iac_pr \
            --run-label self_hosted_bundle \
            --pipeline-source "${CFG_PIPELINE_SOURCE}-iac-pr" \
            --branch "${GITHUB_REF_NAME}" \
            --commit-sha "${GITHUB_SHA}"

      - name: Build remediation metrics
        if: env.CFG_ENABLE_RUNTIME_REMEDIATION == 'true' || env.CFG_RECONCILE_M5_DRIFT == 'true' || env.CFG_GENERATE_IAC_PR == 'true'
        run: |
          PYTHON_BIN="$(command -v python)"
          REMEDIATION_ARGS=()
          if [[ -f ./artifacts/remediation/aws_runtime_events.json ]]; then
            REMEDIATION_ARGS+=(--remediation-events ./artifacts/remediation/aws_runtime_events.json)
          fi
          if [[ -f ./artifacts/remediation/openstack_runtime_events.json ]]; then
            REMEDIATION_ARGS+=(--remediation-events ./artifacts/remediation/openstack_runtime_events.json)
          fi
          if [[ -f ./artifacts/drift/m5/reconcile_events.json ]]; then
            REMEDIATION_ARGS+=(--remediation-events ./artifacts/drift/m5/reconcile_events.json)
          fi
          if [[ -f ./artifacts/iac_pr/self_hosted_bundle/iac_pr_events.json ]]; then
            REMEDIATION_ARGS+=(--remediation-events ./artifacts/iac_pr/self_hosted_bundle/iac_pr_events.json)
          fi
          "$PYTHON_BIN" -m src.remediation.metrics \
            --findings ./scan_results/hybrid_findings.json \
            --decisions ./triage_results/hybrid_decisions.json \
            --output ./artifacts/remediation/hybrid_metrics.json \
            --pipeline-source "${CFG_PIPELINE_SOURCE}-metrics" \
            --branch "${GITHUB_REF_NAME}" \
            --commit-sha "${GITHUB_SHA}" \
            "${REMEDIATION_ARGS[@]}"

      - name: Publish remediation events and metrics
        if: env.CFG_PUBLISH_TO_SIEM == 'true' && (env.CFG_ENABLE_RUNTIME_REMEDIATION == 'true' || env.CFG_RECONCILE_M5_DRIFT == 'true' || env.CFG_GENERATE_IAC_PR == 'true')
        run: |
          PYTHON_BIN="$(command -v python)"
          PUBLISH_ARGS=()
          if [[ -f ./artifacts/remediation/aws_runtime_events.json ]]; then
            PUBLISH_ARGS+=(--remediation-events ./artifacts/remediation/aws_runtime_events.json)
          fi
          if [[ -f ./artifacts/remediation/openstack_runtime_events.json ]]; then
            PUBLISH_ARGS+=(--remediation-events ./artifacts/remediation/openstack_runtime_events.json)
          fi
          if [[ -f ./artifacts/drift/m5/reconcile_events.json ]]; then
            PUBLISH_ARGS+=(--remediation-events ./artifacts/drift/m5/reconcile_events.json)
          fi
          if [[ -f ./artifacts/remediation/hybrid_metrics.json ]]; then
            PUBLISH_ARGS+=(--metrics ./artifacts/remediation/hybrid_metrics.json)
          fi
          "$PYTHON_BIN" -m src.siem.publisher \
            --pipeline-source "${CFG_PIPELINE_SOURCE}-remediation" \
            --branch "${GITHUB_REF_NAME}" \
            --commit-sha "${GITHUB_SHA}" \
            "${PUBLISH_ARGS[@]}"

      - name: Upload hybrid pipeline artifacts
        uses: actions/upload-artifact@v4
        with:
          name: hybrid-runtime-artifacts
          path: |
            scan_results/
            triage_results/
            artifacts/triage_notifications/
            artifacts/remediation/
            artifacts/iac_pr/
            artifacts/drift/

      - name: Write workflow summary
        run: |
          {
            echo "## Hybrid runtime scan summary"
            echo ""
            echo "- Runner labels: \`self-hosted, linux, openstack\`"
            echo "- Trigger: \`${{ github.event_name }}\`"
            echo "- Pipeline source: \`${CFG_PIPELINE_SOURCE}\`"
            echo "- AWS scan enabled: \`${CFG_INCLUDE_AWS_SCAN}\`"
            echo "- OpenStack scan enabled: \`${CFG_INCLUDE_OPENSTACK_SCAN}\`"
            echo "- OpenStack object storage enabled: \`${CFG_INCLUDE_OPENSTACK_OBJECT_STORAGE}\`"
            echo "- OpenStack activate path: \`${CFG_OPENSTACK_ACTIVATE_PATH}\`"
            echo "- OpenStack RC path: \`${CFG_OPENRC_PATH}\`"
            echo "- Publish to SIEM: \`${CFG_PUBLISH_TO_SIEM}\`"
            echo "- Runtime remediation: \`${CFG_ENABLE_RUNTIME_REMEDIATION}\`"
            echo "- M5 drift reconcile: \`${CFG_RECONCILE_M5_DRIFT}\`"
          } >> "$GITHUB_STEP_SUMMARY"
